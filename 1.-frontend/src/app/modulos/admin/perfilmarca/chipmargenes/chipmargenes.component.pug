
mat-form-field(appearance="outline")
	mat-label Margenes
	mat-chip-list(#chipList)
		mat-chip(
			*ngFor="let item of chips",
			[selectable]="selectable",
			[removable]="removable",
			(removed)="remove(item)") {{item.nombre}} | {{item.cantidad}}%
			
			mat-icon(matChipRemove, *ngIf="removable") cancel
		input(
			placeholder="Agregar nuevo margen",
			#input,
			[formControl]="fruitCtrl",
			[matAutocomplete]="auto",
			[matChipInputFor]="chipList",
			[matChipInputSeparatorKeyCodes]="separatorKeysCodes",
			[matChipInputAddOnBlur]="addOnBlur",
			(matChipInputTokenEnd)="add($event)"
			)
		mat-autocomplete(#auto="matAutocomplete", (optionSelected)="selected($event)")
			mat-option(*ngFor="let item of filtered | async", [value]="item.nombre")  {{item.nombre}} | {{item.cantidad}}%

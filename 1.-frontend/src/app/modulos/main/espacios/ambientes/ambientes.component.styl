@import  '../../../../../defaults.styl'

mat-grid-tile
    overflow: hidden
    cursor:  pointer;
    .imagen
        width 100%
        height 100%
        background-size: cover;
        background-position: center;
        transition(500ms, 0)
    &:hover
        .imagen
            transform:  scale(1.1);
